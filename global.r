require(shiny)
require(shinydashboard)
require(leaflet)
require(ggplot2)
require(plyr)

# pi : pw 99*****s!

# /etc/wpa_supplicant/
# /etc/apache2/apache2.conf
# ifconfig
# raspi_config
# a2enmod
# chmod -x
# /etc/rc.local
# /etc/apache2/sites_available